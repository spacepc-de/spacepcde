# A generated secret for Payload
PAYLOAD_SECRET=
OPENAI_API_KEY=
OPENAI_TRANSLATION_MODEL=gpt-5.2
# Optional: comma-separated list of user emails allowed to trigger AI admin routes.
# In production, an empty list blocks non-admin users for these routes.
OPENAI_ADMIN_EMAIL_ALLOWLIST=
